{{- if .Values.onlyoffice.ingress.enabled }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "onlyoffice.fullname" . }}
  labels:
    app: {{ template "name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    kubernetes.io/ingress.class: nginx
{{- if .Values.onlyoffice.ingress.tls }}
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
{{- end }}
{{- if .Values.onlyoffice.ingress.letsEncrypt }}
    certmanager.k8s.io/cluster-issuer: letsencrypt
    kubernetes.io/tls-acme: 'true'
    nginx.ingress.kubernetes.io/tls-acme: 'true'
{{- end }}
    nginx.ingress.kubernetes.io/proxy-body-size: 10G
spec:
{{- if .Values.onlyoffice.ingress.tls }}
  tls:
  - hosts:
    - {{ .Values.onlyoffice.ingress.serverName }}
    secretName: {{ .Values.onlyoffice.ingress.tlsSecretName | default (printf "%s-%s" "tls" (include "onlyoffice.fullname" .)) }}
{{- end }}
  rules:
  - host: {{ .Values.onlyoffice.ingress.serverName }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ template "onlyoffice.fullname" . }}
          servicePort: {{ .Values.onlyoffice.service.port | default "80" }}
{{- end }}